---
- name: Common | SSH keys | Set {{ user.key }} keys
  authorized_key:
    user: "{{ user.value.name }}"
    key: "{{ item.name }}"
    state: "{{ item.state }}"
  with_items: "{{ user.values.ssh_keys }}"
  when: user.values.ssh_keys is defined
